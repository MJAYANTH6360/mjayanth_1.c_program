#include <stdio.h>

int main() {
    int count = 1;  // Initialize the counter variable

    while (count <= 5) {  // Condition to check before each iteration
        printf("%d\n", count);  // Print the current value of count
        count++;  // Increment the counter
    }

    return 0;  // Exit the program
}
